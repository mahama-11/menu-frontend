# Default base URLs for production / remote deployment (relative paths, so Nginx can proxy them)
VITE_MENU_API_BASE_URL=/api/v1/menu
VITE_PLATFORM_API_BASE_URL=/api/v1/platform

# Frontend Application URL (used for sharing links like referrals)
VITE_APP_URL=http://localhost:5173